The trained model file (ids_model.pkl) is not included in this repository due to file size limitations.

The model was trained using a Random Forest Classifier on the CIC-IDS dataset and achieved 99.89% accuracy.
